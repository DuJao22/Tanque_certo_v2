# Server Configuration
PORT=3000
JWT_SECRET=your_super_secret_jwt_key_here
DATABASE_PATH=tanque_certo.db

# Note: On Render, if you use a Persistent Disk, 
# set DATABASE_PATH to the mount path (e.g., /data/tanque_certo.db)
